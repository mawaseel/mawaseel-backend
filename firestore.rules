rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // كل Firestore مقفول عن تطبيقات الويب/الموبايل بالكامل.
    // القراءة والكتابة تتم فقط من Backend باستخدام Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
